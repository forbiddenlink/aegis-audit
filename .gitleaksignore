# Fingerprints gitleaks should not flag. These are deliberately fake AWS key
# IDs used as test fixtures to prove the secret detectors fire -- AWS's own
# documented example key ID and a throwaway, not real credentials. See
# tests/test_secret_patterns.py.
tests/test_secret_patterns.py:aws-access-token:25
